package com.lighthouse.navigator

import android.content.Context
import androidx.activity.result.ActivityResult
import com.lighthouse.features.common.navigator.AddGifticonNavigator
import javax.inject.Inject

internal class AddGifticonNavigatorImpl @Inject constructor() : AddGifticonNavigator {

    override fun openAddGifticon(context: Context, result: (ActivityResult) -> Unit) {
        TODO("Not yet implemented")
    }
}
