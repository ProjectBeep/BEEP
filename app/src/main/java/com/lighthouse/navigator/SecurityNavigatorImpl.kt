package com.lighthouse.navigator

import android.content.Context
import com.lighthouse.features.common.navigator.SecurityNavigator
import javax.inject.Inject

internal class SecurityNavigatorImpl @Inject constructor() : SecurityNavigator {

    override fun openSecurity(context: Context) {
        TODO("Not yet implemented")
    }
}
