package com.lighthouse.navigator

import android.app.Activity
import com.lighthouse.features.common.navigator.GifticonListNavigator
import javax.inject.Inject

internal class GifticonListNavigatorImpl @Inject constructor() : GifticonListNavigator {

    override fun openList(activity: Activity) {
        TODO("Not yet implemented")
    }
}
