<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main_graph"
    app:startDestination="@id/main_nav_graph">

    <include
        android:id="@+id/main_nav_graph"
        app:graph="@navigation/main_nav_graph" />

    <include
        android:id="@+id/intro_nav_graph"
        app:graph="@navigation/intro_nav_graph" />

    <include
        android:id="@+id/coffee_nav_graph"
        app:graph="@navigation/coffee_nav_graph" />

    <include
        android:id="@+id/open_source_license_nav_graph"
        app:graph="@navigation/open_source_license_nav_graph" />

    <include
        android:id="@+id/personal_info_policy_nav_graph"
        app:graph="@navigation/personal_info_policy_nav_graph" />

    <include
        android:id="@+id/security_nav_graph"
        app:graph="@navigation/security_nav_graph" />

    <include
        android:id="@+id/terms_of_use_nav_graph"
        app:graph="@navigation/terms_of_use_nav_graph" />

    <include
        android:id="@+id/used_gifticon_nav_graph"
        app:graph="@navigation/used_gifticon_nav_graph" />

    <action
        android:id="@+id/action_global_main_graph"
        app:destination="@id/main_nav_graph"
        app:popUpTo="@id/intro_nav_graph"
        app:popUpToInclusive="true" />

    <action
        android:id="@+id/action_global_intro_graph"
        app:destination="@id/intro_nav_graph"
        app:popUpTo="@id/main_nav_graph"
        app:popUpToInclusive="true" />
</navigation>